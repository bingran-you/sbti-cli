{
  "name": "sbti-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone CLI runner for the public SBTI survey.",
  "bin": {
    "sbti-cli": "./src/cli.mjs"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "sbti": "node src/cli.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
